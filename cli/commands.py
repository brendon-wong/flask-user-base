"""Click commands."""

# import click
# from flask import Flask

"""
@click.command()
def test():
    # Run the tests.
    import pytest
    rv = pytest.main([TEST_PATH, '--verbose'])
    exit(rv)
"""